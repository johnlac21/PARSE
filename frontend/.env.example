# Backend API base URL. Leave unset or empty to use same-origin proxy (next.config.js forwards /api to backend).
# Set to e.g. http://localhost:8000 only if you want the browser to call the backend directly.
# NEXT_PUBLIC_API_URL=http://localhost:8000
